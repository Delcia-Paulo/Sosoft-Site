export default function Modal() {
   const [showModal, setShowModal] = useState(false);

  function OpenModal() {
    setShowModal(true);
  }
  function CloseModal() {
    setShowModal(false);
  }
    return(

    )
}